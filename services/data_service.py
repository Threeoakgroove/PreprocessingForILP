class DataService:
    'Service to load a file of gps points.'

    def loadHelloWorld(self):
        return ('Hello World!')
        # gpsPoints = []
        # gpsPointLines = open(path).readlines()

        # for gpsPointLine in gpsPointLines[6:]:
        #     gpsPoints.append(self.createPoint(gpsPointLine))

        # featuredGpsPoints = self.getPointsFeatures(gpsPoints)
        # accelGpsPoints = self.calculateAccelerations(featuredGpsPoints)
        # labeledGpsPoints = self.estimateLabels(accelGpsPoints)

        # return accelGpsPoints
